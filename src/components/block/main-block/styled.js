import styled from "styled-components";

export const BlockCreateTask = styled.div `
`

export const BlockTasks = styled.div`
    height: calc(100vh - 508px);
    overflow-y: scroll;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px;
    background-color: #ececec;
    margin-bottom: 40px;
`
